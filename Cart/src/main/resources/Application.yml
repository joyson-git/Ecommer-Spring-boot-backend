server:
  port: 8086

spring:
  application:
    name: CART-SERVICE

  data:
    mongodb:
      uri: mongodb://localhost:27017/cart_db

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/