server:
  port: 8089

spring:
  application:
    name: ORDER-SERVICE

  data:
    mongodb:
      uri: mongodb://localhost:27017/order_db

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/