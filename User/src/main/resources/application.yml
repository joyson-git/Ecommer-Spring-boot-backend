server:
  port: 8081

spring:
  application:
    name: USER-SERVICE

  data:
    mongodb:
      uri: mongodb://localhost:27017/ecomm_user

      eureka:
        client:
          service-url:
            defaultZone: http://localhost:8761/eureka